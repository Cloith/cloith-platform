apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chatter-route
  namespace: chatter-dev
spec:
  parentRefs:
    - name: nginx-gateway
      namespace: default
  hostnames:
    - "srv1154036.hstgr.cloud"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: chatter-frontend-service
          port: 80